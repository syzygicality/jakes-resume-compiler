services:
  api:
    build: .
    ports:
      - "8080:8080"
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - PROD=true
  api-image:
    platform: linux/amd64
    image: ghcr.io/syzygicality/jakes-resume-compiler:latest
    ports:
      - "8081:8080"
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - PROD=true
